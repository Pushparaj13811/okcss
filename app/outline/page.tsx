import { OutlineGenerator } from '@/src/components/outline/OutlineGenerator';

export const metadata = {
  title: 'Outline — ok.css',
  description:
    'Generate CSS outline and outline-offset for focus-visible styles. Perfect for accessible interactive element design.',
};

export default function OutlinePage() {
  return <OutlineGenerator />;
}
