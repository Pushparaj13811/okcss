import { BorderRadiusGenerator } from '@/src/components/border-radius/BorderRadiusGenerator';

export const metadata = {
  title: 'Border Radius — ok.css',
  description: 'Generate CSS border-radius with uniform or per-corner control.',
};

export default function BorderRadiusPage() {
  return <BorderRadiusGenerator />;
}
