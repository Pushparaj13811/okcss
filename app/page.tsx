import { LandingPage } from '@/src/components/landing/LandingPage';

export const metadata = {
  title: 'ok.css — CSS generators that don\'t get in your way',
  description: 'Visual CSS generators for shadow, glassmorphism, gradient, and neumorphism. Tweak in real-time, copy CSS or Tailwind.',
};

export default function Home() {
  return <LandingPage />;
}
