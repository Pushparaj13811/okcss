import { TextShadowGenerator } from '@/src/components/text-shadow/TextShadowGenerator';

export const metadata = {
  title: 'Text Shadow — ok.css',
  description: 'Generate CSS text-shadow declarations with live preview.',
};

export default function TextShadowPage() {
  return <TextShadowGenerator />;
}
